const NewProof = () => (
  <div className="min-h-screen flex items-center justify-center" style={{ backgroundColor: "#080808" }}>
    <p className="font-body" style={{ color: "rgba(240,240,238,0.45)" }}>New Proof — placeholder</p>
  </div>
);

export default NewProof;
